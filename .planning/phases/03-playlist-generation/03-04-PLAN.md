# Phase 3 Plan 03-04: Conservative Clustering + Tuning

## Goal
Reduce false positives with conservative clustering and tunable thresholds.

## Tasks
1. Add threshold presets (Conservative / Balanced / Exploratory).
2. Add max-pairs guardrails per cluster generation run.
3. Add debug stats per run (pairs computed, cache hits, elapsed).

## Success
- Conservative preset yields fewer but higher-confidence clusters.
