---
phase: 05-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - renderer/styles/design-system.css
  - renderer/styles/globals.css
  - renderer/index.html
autonomous: true

must_haves:
  truths:
    - Dark theme applied throughout application
    - CSS variables accessible to all components
    - Inter font loaded and applied
    - Color palette consistent across UI
  artifacts:
    - renderer/styles/design-system.css
    - renderer/styles/globals.css
  key_links:
    - Design system variables imported in App.jsx
    - Font loaded via index.html or CSS import
---

<objective>
Establish design system foundation with CSS variables, dark theme, typography, and spacing system.

Purpose: Create reusable design tokens that all components will reference for consistent styling.
Output: CSS files with complete design system (colors, typography, spacing, shadows, transitions).
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/UI-REDESIGN-PLAN.md

# Current renderer structure
@renderer/App.jsx
@renderer/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design system CSS variables</name>
  <files>renderer/styles/design-system.css</files>
  <action>
Create comprehensive design system with CSS custom properties:

**Color Palette:**
```css
:root {
  /* Base - Dark Theme */
  --bg-primary: #0a0e14;
  --bg-secondary: #111827;
  --bg-tertiary: #1e293b;
  --bg-elevated: #1f2937;

  --text-primary: #e2e8f0;
  --text-secondary: #94a3b8;
  --text-tertiary: #64748b;

  --border-subtle: #1e293b;
  --border-medium: #334155;

  /* Accent Colors */
  --accent-primary: #3b82f6;
  --accent-glow: #60a5fa;
  --accent-success: #10b981;
  --accent-warning: #f59e0b;
  --accent-error: #ef4444;

  /* Gradients */
  --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --gradient-waveform: linear-gradient(180deg, #3b82f6 0%, #1e40af 100%);

  /* Waveform */
  --waveform-blue: #3b82f6;
  --waveform-purple: #8b5cf6;
  --waveform-cyan: #06b6d4;

  /* Typography */
  --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', Consolas, monospace;

  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.25rem;
  --text-2xl: 1.5rem;
  --text-3xl: 1.875rem;

  --weight-normal: 400;
  --weight-medium: 500;
  --weight-semibold: 600;
  --weight-bold: 700;

  /* Spacing */
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-12: 3rem;

  /* Border Radius */
  --radius-sm: 0.375rem;
  --radius-md: 0.5rem;
  --radius-lg: 0.75rem;
  --radius-xl: 1rem;
  --radius-full: 9999px;

  /* Shadows & Effects */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.5);
  --shadow-glow: 0 0 20px rgba(59, 130, 246, 0.4);

  /* Transitions */
  --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-base: 200ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-slow: 300ms cubic-bezier(0.4, 0, 0.2, 1);
}
```
  </action>
  <verify>
```bash
# File exists
ls renderer/styles/design-system.css

# Contains all variable categories
grep -E "(--bg-|--text-|--accent-|--font-|--space-|--radius-|--shadow-|--transition-)" renderer/styles/design-system.css | wc -l
# Should show 30+ variables
```
  </verify>
  <done>
design-system.css created with complete CSS custom properties for colors, typography, spacing, effects
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply dark theme global styles</name>
  <files>renderer/styles/globals.css</files>
  <action>
Create global styles that apply dark theme:

```css
@import './design-system.css';

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html,
body {
  font-family: var(--font-sans);
  font-size: var(--text-base);
  font-weight: var(--weight-normal);
  line-height: 1.5;
  color: var(--text-primary);
  background-color: var(--bg-primary);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

#root {
  min-height: 100vh;
}

/* Scrollbar styling */
::-webkit-scrollbar {
  width: 12px;
  height: 12px;
}

::-webkit-scrollbar-track {
  background: var(--bg-secondary);
}

::-webkit-scrollbar-thumb {
  background: var(--border-medium);
  border-radius: var(--radius-sm);
}

::-webkit-scrollbar-thumb:hover {
  background: var(--accent-primary);
}

/* Selection */
::selection {
  background: var(--accent-primary);
  color: white;
}

/* Focus outline */
:focus-visible {
  outline: 2px solid var(--accent-primary);
  outline-offset: 2px;
}

/* Smooth scrolling */
html {
  scroll-behavior: smooth;
}
```
  </action>
  <verify>
```bash
# File exists
ls renderer/styles/globals.css

# Imports design system
grep "@import.*design-system" renderer/styles/globals.css

# Applies dark theme
grep "background-color: var(--bg-primary)" renderer/styles/globals.css
```
  </verify>
  <done>
globals.css created with dark theme, custom scrollbars, focus states, smooth scrolling
  </done>
</task>

<task type="auto">
  <name>Task 3: Load Inter font and import global styles</name>
  <files>renderer/index.html, renderer/main.jsx</files>
  <action>
Load Inter font from Google Fonts and ensure global styles are imported.

**In renderer/index.html** (add to `<head>`):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

**In renderer/main.jsx** (add import at top):
```javascript
import './styles/globals.css';
```

Create renderer/styles/ directory if it doesn't exist.
  </action>
  <verify>
```bash
# Styles directory exists
ls -d renderer/styles/

# Font link in HTML
grep "fonts.googleapis.com.*Inter" renderer/index.html

# Global styles imported
grep "import.*globals.css" renderer/main.jsx
```
  </verify>
  <done>
Inter font loaded via Google Fonts, global styles imported in main entry point
  </done>
</task>

</tasks>

<success_criteria>
**Acceptance:**
1. Dark theme visible when app loads (background: #0a0e14)
2. Inter font applied to all text
3. CSS variables accessible in DevTools (inspect :root)
4. Custom scrollbars show dark theme
5. Text selection shows blue highlight

**Testing:**
```bash
# Start app
npm run dev:renderer
npm run start:electron:safe

# Visual checks in running app:
# 1. Background should be deep dark navy (#0a0e14)
# 2. Text should be light gray (#e2e8f0)
# 3. Font should be Inter (check DevTools computed styles)
# 4. Scroll any list - scrollbar should be dark with blue thumb on hover
# 5. Select text - should have blue highlight
# 6. Tab through elements - blue focus outline visible
```
</success_criteria>

<must_have_derivation>
**Goal:** Establish design foundation for consistent modern dark UI

**Critical behaviors:**
- All components inherit dark theme from global styles
- CSS variables enable consistent styling across components
- Typography system ensures readable hierarchy
- Spacing system provides consistent layout

**Critical artifacts:**
- design-system.css (design tokens)
- globals.css (theme application)

**Critical connections:**
- Design system imported in globals
- Globals imported in main.jsx
- Inter font loaded from Google Fonts
- Variables accessible to all components via :root
</must_have_derivation>
