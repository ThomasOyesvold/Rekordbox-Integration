---
phase: 05-ui-redesign
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - package.json
  - renderer/components/ui/Button.jsx
  - renderer/components/ui/Card.jsx
  - renderer/components/ui/Input.jsx
  - renderer/styles/components.css
autonomous: true

must_haves:
  truths:
    - Buttons show gradient backgrounds with glow on hover
    - Cards have elevated appearance with subtle borders
    - Inputs have dark backgrounds with blue glow on focus
    - All components use design system variables
  artifacts:
    - renderer/components/ui/Button.jsx
    - renderer/components/ui/Card.jsx
    - renderer/components/ui/Input.jsx
    - renderer/styles/components.css
  key_links:
    - Components import design system variables
    - Framer Motion installed for animations
    - Lucide React installed for icons
---

<objective>
Build reusable UI component library (Button, Card, Input) with modern styling and animations.

Purpose: Create consistent, polished components that all features will use.
Output: Base UI components with hover effects, transitions, and proper styling.
</objective>

<execution_context>
@/home/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/home/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/UI-REDESIGN-PLAN.md
@.planning/phases/05-ui-redesign/05-01-PLAN.md

# Design system from Plan 01
@renderer/styles/design-system.css
@renderer/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install animation and icon libraries</name>
  <files>package.json</files>
  <action>
Install Framer Motion for animations and Lucide React for icons:

```bash
npm install framer-motion lucide-react
```

These provide:
- Framer Motion: Smooth animations, gesture support, layout animations
- Lucide React: Clean, modern icon set (Music, Play, Settings, etc.)
  </action>
  <verify>
```bash
# Dependencies installed
grep -E "(framer-motion|lucide-react)" package.json

# Modules exist
ls node_modules/framer-motion node_modules/lucide-react
```
  </verify>
  <done>
framer-motion and lucide-react installed in package.json dependencies
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Button component</name>
  <files>renderer/components/ui/Button.jsx</files>
  <action>
Create modern Button component with variants (primary, secondary, icon):

```jsx
import { motion } from 'framer-motion';
import './Button.css';

export function Button({
  children,
  variant = 'primary',
  size = 'md',
  icon,
  onClick,
  disabled = false,
  className = '',
  ...props
}) {
  const baseClass = 'btn';
  const variantClass = `btn-${variant}`;
  const sizeClass = `btn-${size}`;

  return (
    <motion.button
      className={`${baseClass} ${variantClass} ${sizeClass} ${className}`}
      onClick={onClick}
      disabled={disabled}
      whileHover={{ scale: disabled ? 1 : 1.02 }}
      whileTap={{ scale: disabled ? 1 : 0.98 }}
      transition={{ duration: 0.15 }}
      {...props}
    >
      {icon && <span className="btn-icon">{icon}</span>}
      <span className="btn-text">{children}</span>
    </motion.button>
  );
}

export function IconButton({ icon, onClick, className = '', ...props }) {
  return (
    <motion.button
      className={`icon-button ${className}`}
      onClick={onClick}
      whileHover={{ scale: 1.1 }}
      whileTap={{ scale: 0.9 }}
      transition={{ duration: 0.15 }}
      {...props}
    >
      {icon}
    </motion.button>
  );
}
```

**Companion CSS** (renderer/components/ui/Button.css):
```css
.btn {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-3) var(--space-6);
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
  border: none;
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all var(--transition-base);
  font-family: inherit;
}

.btn-primary {
  background: var(--gradient-primary);
  color: white;
  box-shadow: var(--shadow-sm);
}

.btn-primary:hover:not(:disabled) {
  box-shadow: var(--shadow-glow);
  filter: brightness(1.1);
}

.btn-secondary {
  background: transparent;
  color: var(--text-primary);
  border: 1px solid var(--border-medium);
}

.btn-secondary:hover:not(:disabled) {
  background: var(--bg-tertiary);
  border-color: var(--accent-primary);
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.icon-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  border: none;
  border-radius: var(--radius-full);
  background: transparent;
  color: var(--text-secondary);
  cursor: pointer;
  transition: all var(--transition-base);
}

.icon-button:hover {
  background: var(--bg-tertiary);
  color: var(--accent-primary);
}
```
  </action>
  <verify>
```bash
# Component exists
ls renderer/components/ui/Button.jsx

# Imports Framer Motion
grep "from 'framer-motion'" renderer/components/ui/Button.jsx

# Has variants
grep -E "(btn-primary|btn-secondary|icon-button)" renderer/components/ui/Button.jsx
```
  </verify>
  <done>
Button component created with primary/secondary/icon variants, Framer Motion animations, hover effects
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Card and Input components</name>
  <files>renderer/components/ui/Card.jsx, renderer/components/ui/Input.jsx</files>
  <action>
Create Card and Input components following design system.

**Card.jsx:**
```jsx
import { motion } from 'framer-motion';
import './Card.css';

export function Card({ children, className = '', hoverable = false, ...props }) {
  return (
    <motion.div
      className={`card ${hoverable ? 'card-hoverable' : ''} ${className}`}
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.2 }}
      whileHover={hoverable ? { y: -2, boxShadow: '0 10px 30px rgba(0,0,0,0.5)' } : {}}
      {...props}
    >
      {children}
    </motion.div>
  );
}

export function CardHeader({ children, className = '' }) {
  return <div className={`card-header ${className}`}>{children}</div>;
}

export function CardContent({ children, className = '' }) {
  return <div className={`card-content ${className}`}>{children}</div>;
}

export function CardFooter({ children, className = '' }) {
  return <div className={`card-footer ${className}`}>{children}</div>;
}
```

**Card.css:**
```css
.card {
  background: var(--bg-secondary);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-md);
  overflow: hidden;
}

.card-hoverable {
  transition: all var(--transition-base);
  cursor: pointer;
}

.card-header {
  padding: var(--space-6);
  border-bottom: 1px solid var(--border-subtle);
}

.card-content {
  padding: var(--space-6);
}

.card-footer {
  padding: var(--space-6);
  border-top: 1px solid var(--border-subtle);
  background: var(--bg-tertiary);
}
```

**Input.jsx:**
```jsx
import './Input.css';

export function Input({
  label,
  hint,
  error,
  className = '',
  ...props
}) {
  return (
    <div className={`input-group ${className}`}>
      {label && <label className="input-label">{label}</label>}
      <input
        className={`input ${error ? 'input-error' : ''}`}
        {...props}
      />
      {hint && !error && <span className="input-hint">{hint}</span>}
      {error && <span className="input-error-text">{error}</span>}
    </div>
  );
}
```

**Input.css:**
```css
.input-group {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.input-label {
  font-size: var(--text-sm);
  font-weight: var(--weight-medium);
  color: var(--text-primary);
}

.input {
  padding: var(--space-3) var(--space-4);
  font-size: var(--text-base);
  font-family: inherit;
  color: var(--text-primary);
  background: var(--bg-tertiary);
  border: 1px solid var(--border-medium);
  border-radius: var(--radius-sm);
  transition: all var(--transition-base);
}

.input:focus {
  outline: none;
  border-color: var(--accent-primary);
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

.input-error {
  border-color: var(--accent-error);
}

.input-hint {
  font-size: var(--text-xs);
  color: var(--text-tertiary);
}

.input-error-text {
  font-size: var(--text-xs);
  color: var(--accent-error);
}
```
  </action>
  <verify>
```bash
# Components exist
ls renderer/components/ui/Card.jsx renderer/components/ui/Input.jsx

# Card has subcomponents
grep -E "(CardHeader|CardContent|CardFooter)" renderer/components/ui/Card.jsx

# Input has label/hint/error support
grep -E "(label|hint|error)" renderer/components/ui/Input.jsx
```
  </verify>
  <done>
Card and Input components created with proper styling, animations, error states
  </done>
</task>

</tasks>

<success_criteria>
**Acceptance:**
1. Button component renders with gradient (primary) or border (secondary)
2. Buttons scale on hover (1.02) and press (0.98)
3. Cards have elevated appearance with subtle border
4. Hoverable cards lift on hover
5. Inputs show blue glow on focus
6. All components use CSS variables from design system

**Testing:**
```bash
# Test in App.jsx temporarily:
import { Button, IconButton } from './components/ui/Button';
import { Card, CardHeader, CardContent } from './components/ui/Card';
import { Input } from './components/ui/Input';
import { Play, Settings } from 'lucide-react';

// Add to render:
<Card hoverable>
  <CardHeader>
    <h3>Test Card</h3>
  </CardHeader>
  <CardContent>
    <Input label="Library Path" hint="Or drag and drop" />
    <Button variant="primary" icon={<Play size={16} />}>
      Play Track
    </Button>
    <Button variant="secondary">Cancel</Button>
    <IconButton icon={<Settings size={20} />} />
  </CardContent>
</Card>

# Visual checks:
# 1. Primary button has gradient and glows on hover
# 2. Card lifts on hover
# 3. Input shows blue glow when focused
# 4. Icons render from Lucide React
# 5. All animations smooth
```
</success_criteria>

<must_have_derivation>
**Goal:** Reusable component library with modern styling

**Critical behaviors:**
- Components respond to interactions (hover, focus, click)
- Animations smooth and performant (60fps)
- Styling consistent with design system
- Components composable (Card + CardHeader + CardContent)

**Critical artifacts:**
- Button.jsx (primary/secondary/icon variants)
- Card.jsx (with subcomponents)
- Input.jsx (with label/hint/error)
- Companion CSS files

**Critical connections:**
- All components use design system CSS variables
- Framer Motion provides smooth animations
- Lucide React provides icon system
- Components exported for use throughout app
</must_have_derivation>
