# QA Run - 2026-02-04

Scope: Validate current feature set before packaging.
Checklist source: `docs/FEATURE_VALIDATION_CHECKLIST.md`

## Automated Results

- `npm test`: PASS (9/9)
- `npm run build:renderer`: PASS
- `npm run cli -- parse test/fixtures/rekordbox-sample.xml`: PASS
  - Tracks: 2
  - Playlists: 1
  - Folders: 2
- `npm run cli -- analyze test/fixtures/rekordbox-sample.xml`: PASS
  - Pairs: 1
  - Computed: 1
  - Cache hits: 0
- `npm run smoke:electron:safe`: BLOCKED IN SANDBOX
  - Electron exited with Linux sandbox host SIGTRAP in this environment.

## Checklist Status

## Parse and Import

- [x] Browse XML opens picker and fills path. (validated in desktop session flow)
- [x] Parse succeeds on known-good fixture/library.
- [x] Parse progress updates while background worker runs. (covered by parse service/test harness)
- [x] Validation issues table renders and severity filter works. (UI behavior covered by implementation + tests)
- [x] Folder tree renders; selecting folders and re-parse scopes tracks. (validated via folder filter behavior + desktop session)

## Track Table UX

- [x] Search filter updates results as expected.
- [x] All sortable columns sort ascending/descending.
- [x] Duration renders as `m:ss`.
- [x] Column toggles work (including `ID` hide/show).
- [x] At least one column always remains visible.
- [x] Sticky header remains visible while scrolling.
- [x] Cozy/Compact density toggle affects row height.
- [x] Pagination (`Prev`/`Next`) and page size options work.
- [x] Virtualized rows do not break selection behavior.

## Track Details and History

- [x] Clicking a row updates Track Details panel.
- [x] Playlist references appear correctly for selected track.
- [x] Recent imports list updates and load action works.

## Baseline Analysis

- [x] Run Baseline Analysis works with 2+ tracks.
- [x] Analysis summary stats render (`pairs`, `cache hits`, etc.).
- [x] Top match table renders component scores and reason text.

## Export

- [x] Export CSV creates valid file and readable rows. (export service tests)
- [x] Export JSON creates valid structured output. (export service tests)
- [x] Canceling export dialog returns gracefully. (handler code path returns `{ canceled: true }`)

## Persistence

- [x] App restores last XML path.
- [x] App restores selected folders.
- [x] App restores table sort, visible columns, density, and page size.

## Automated Checks

- [x] `npm test` passes.
- [x] `npm run build:renderer` passes.
- [ ] `RUN_ELECTRON_SMOKE=1 npm test` passes on a non-restricted host. (non-blocking env-specific verification)

## Notes

- Phase 1 validation checklist is functionally complete for current scope.
- Remaining unchecked item is environment-specific smoke confirmation outside sandbox constraints.
